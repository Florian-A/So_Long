{
    "version": "2.0.0",
    "tasks": [
        {
            "type" : "shell",
            "label": "build",
            "command": "make",
            "args": [
                "DEBUG=1"
            ],
            "options": {
                "cwd": "${workspaceFolder}/"
              }
            "isShellCommand": true,
        }
    ]
}